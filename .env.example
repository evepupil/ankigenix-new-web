# ==========================================
# Supabase 配置
# ==========================================
# 从 Supabase Dashboard > Settings > API 获取以下配置
# https://app.supabase.com/project/_/settings/api

# 1. Supabase 项目 URL（必需）
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# 2. Supabase Anon Key（必需 - 前端使用）
# ⚠️ 这个 key 是公开的，会暴露在浏览器中，这是正常的！
# 安全性由 Row Level Security (RLS) 保证
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 3. Supabase Service Role Key（可选 - 仅后端使用）
# ❌ 绝对不要在前端使用！只能在服务端 API 路由中使用
# ❌ 不要添加 NEXT_PUBLIC_ 前缀！
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ==========================================
# 后端 API 配置（可选）
# ==========================================
# 如果你的 Django 后端部署在独立服务器
# NEXT_PUBLIC_API_URL=http://localhost:8000/api
# 如果不配置，将使用 Next.js 的 API 路由
